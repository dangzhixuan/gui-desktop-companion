# 第六阶段：角色前台与轻量后台

这一阶段把产品入口从“大窗口应用”调整为“桌面角色”：

- 启动后只显示陆文昔，管理中心默认隐藏；
- 问候和普通任务提醒只通过气泡展示；
- 没有今日任务时，气泡提供“填写今日任务”按钮；
- 晚间复盘时，气泡提供“去反馈”按钮；
- 按钮会直接打开后台对应页面；
- 普通任务提醒按设置中的间隔周期触发，不打断当前工作。

后台窗口缩小为管理中心，继续使用本地 SQLite，负责：

- 任务录入与状态管理；
- 晚间反馈和总结；
- 历史记录；
- 时间与人格设置。

日常使用不需要一直打开后台窗口。
